# Project docs

This folder groups the reusable non-code docs for Dali House by role.

## Structure

- `docs/brand/` for brand identity and voice
- `docs/workflows/` for content and publishing workflows
- `docs/social/` for social media guidance

Keep `AGENTS.md` and `CLAUDE.md` at the repo root because tooling expects them there.
