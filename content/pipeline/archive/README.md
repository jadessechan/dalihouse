# content/pipeline/archive

Use this folder only if you want to preserve workflow artifacts after a post is published.

Suggested shape:
- `content/pipeline/archive/<year>/<slug>/`

If you do not need the workflow history, you can skip archiving and simply remove the post from `active/` after publish.
